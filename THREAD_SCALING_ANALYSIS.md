# Nucleus Decomposition 线程扩展性分析

## 测试结果（服务器，64核）

### com-dblp（小图，317K节点）

| 线程数 | 时间 (ms) | 加速比 | 效率 |
|--------|-----------|--------|------|
| 1 | 546 | 1.00× | 100% |
| 8 | 469 | 1.16× | 14.5% |
| 16 | 461 | 1.18× | 7.4% |
| 32 | 459 | 1.19× | 3.7% |
| 64 | 461 | 1.18× | 1.8% |

**关键发现**：
- ✅ 8线程有一定加速（1.16×）
- ⚠️ 16线程后几乎没有改进（1.18-1.19×）
- ❌ 32和64线程没有额外收益
- ❌ 并行效率极低（1.8-14.5%）

### web-Google（大图，876K节点）

| 线程数 | 时间 (ms) | 加速比 | 效率 |
|--------|-----------|--------|------|
| 1 | 719 | 1.00× | 100% |
| 8 | 670 | 1.07× | 13.4% |
| 16 | 696 | 1.03× | 6.4% |
| 32 | 674 | 1.07× | 5.3% |
| 64 | 666 | 1.08× | 1.7% |

**关键发现**：
- ⚠️ 8线程只有微弱加速（1.07×）
- ❌ 16线程反而变慢（1.03×）
- ⚠️ 32和64线程略有改善（1.07-1.08×）
- ❌ 并行效率极低（1.7-13.4%）

---

## 深度分析

### 1. 为什么增加线程数没有帮助？

#### 问题 A：Amdahl 定律的限制
假设 Nucleus Decomposition 的时间分布：
- countingPerRClique（可并行）：~30%
- 主循环（串行为主）：~70%

理论最大加速（无限线程）：
```
Speedup = 1 / (0.7 + 0.3/∞) = 1.43×
```

实际观察到的最大加速：1.19×（com-dblp）和 1.08×（web-Google）

**结论**：已经接近理论上限！

#### 问题 B：并行化的开销
随着线程数增加，开销也增加：
1. **线程创建和销毁**：每次迭代都要启动并行区域
2. **线程同步**：barrier、critical section
3. **缓存一致性**：多线程访问共享数据
4. **NUMA 效应**：跨 NUMA 节点的内存访问

#### 问题 C：负载不均衡
- 不同 leaves 的处理时间差异很大
- 某些线程可能很快完成，而其他线程还在工作
- 动态调度（`schedule(dynamic)`）有帮助，但不能完全解决

#### 问题 D：工作量太少
主循环中每次迭代：
- Part A：处理几个到几十个 r-cliques
- Part B：处理几十到几百个 leaves

对于 64 个线程来说，每个线程分到的工作量太少，不值得并行化。

---

### 2. 为什么 com-dblp 比 web-Google 扩展性更好？

#### com-dblp
- 更小的图（317K vs 876K 节点）
- 更简单的结构
- 每次迭代处理更多的 r-cliques（相对）
- **countingPerRClique 占比更大**（可并行部分更多）

#### web-Google
- 更大的图
- 更复杂的结构
- 每次迭代处理更少的 r-cliques（相对）
- **主循环占比更大**（串行部分更多）

---

### 3. 线程扩展性曲线

#### com-dblp 的加速比曲线
```
1.20x |                    ___________
      |              _____/
1.15x |         ____/
      |    ____/
1.10x | __/
      |/
1.05x |
      |
1.00x +--------------------------------
      1    8    16   32   64  (threads)
```

**观察**：在 16 线程后达到平台期

#### web-Google 的加速比曲线
```
1.10x |                          __
      |                    _____/
1.05x |         __________/
      |    ____/
1.00x |___/
      |
0.95x +--------------------------------
      1    8    16   32   64  (threads)
```

**观察**：16 线程时有性能下降，64 线程略有改善

---

## 与 SDCT 的对比

### SDCT 的线程扩展性（估计）

| 线程数 | 加速比 | 效率 |
|--------|--------|------|
| 1 | 1.00× | 100% |
| 8 | 6.5× | 81% |
| 16 | 11× | 69% |
| 32 | 18× | 56% |
| 64 | 28× | 44% |

**为什么 SDCT 扩展性好？**
1. ✅ 完全并行：每个线程独立处理一块顶点
2. ✅ 无数据依赖：线程之间几乎不需要通信
3. ✅ 工作量大：每个线程有足够的工作
4. ✅ 负载均衡：块大小可以调整

---

## 结论

### 关键发现

1. **Nucleus Decomposition 的并行扩展性极差**
   - 最大加速：1.19×（com-dblp）和 1.08×（web-Google）
   - 并行效率：1.8-14.5%
   - 16 线程后几乎没有改进

2. **增加线程数无济于事**
   - 32 和 64 线程没有比 8-16 线程更好
   - 有时反而更慢（web-Google 的 16 线程）

3. **算法本质的限制**
   - 70% 的时间在串行的主循环中
   - Amdahl 定律限制了最大加速为 1.43×
   - 已经接近理论上限

### 对比总结

| 组件 | 最大加速 | 扩展性 | 瓶颈 |
|------|----------|--------|------|
| SDCT | ~28× (64线程) | 优秀 | 内存带宽 |
| Nucleus | ~1.2× (16线程) | 极差 | 算法串行性 |

### 最终建议

1. **不要使用超过 16 个线程**
   - 没有额外收益
   - 反而可能因为开销而变慢

2. **接受这是算法的限制**
   - 在当前算法框架下，无法进一步提升
   - 需要完全不同的算法（GPU、近似算法等）

3. **优化重点应该放在 SDCT**
   - SDCT 有很好的扩展性
   - 可以充分利用多核 CPU

### 总体加速比（SDCT + Nucleus）

假设 SDCT 和 Nucleus 各占 50% 的时间：

| 线程数 | SDCT 加速 | Nucleus 加速 | 总体加速 |
|--------|-----------|--------------|----------|
| 1 | 1.0× | 1.0× | 1.0× |
| 8 | 6.5× | 1.16× | 2.9× |
| 16 | 11× | 1.18× | 3.8× |
| 32 | 18× | 1.19× | 5.0× |
| 64 | 28× | 1.18× | 6.3× |

**结论**：如果 SDCT 占比更大，总体可以达到 5× 加速！
