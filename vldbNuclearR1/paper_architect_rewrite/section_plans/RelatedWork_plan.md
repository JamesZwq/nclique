# RelatedWork Plan

Section purpose:
Position the work by problem category and limitation.

Reader knowledge before:
The reader has seen the static-CPI pipeline.

Reader knowledge after:
The reader knows why prior work does not already give this exact \(r=1\) state model.

Paragraph contracts:
1. Enumeration-based core and nucleus decomposition.
2. Parallel/local peeling and \(h\)-index methods.
3. Clique counting and compact clique representations.
4. Hierarchical cohesive subgraph extraction.

Key definitions introduced:
None beyond category names.

Claims made:
Prior work either solves general mutable decomposition, enumerates/counts cliques, iterates local cores, or extracts hierarchies after sorting.

Evidence used:
Existing citations in `references.bib`.

Sentence-level risks:
Do not overstate prior-work limitations.
Make the contrast precise.
