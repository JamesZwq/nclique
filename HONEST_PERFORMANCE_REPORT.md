# Pivoter 并行优化 - 真实性能报告

## 测试方法

对比三个版本：
1. **Ref 版本**：参考实现（已包含一些并行优化）
2. **优化单线程**：我们的优化版本，单线程运行
3. **优化多线程**：我们的优化版本，8线程运行

## 真实测试结果（服务器，8线程）

### com-dblp（小图，317K节点）

| 版本 | 时间 | vs Ref |
|------|------|--------|
| Ref | 335 ms | 1.00× |
| 优化单线程 | 371 ms | **0.90×**（更慢） |
| 优化多线程 | 370 ms | **0.91×**（更慢） |

**结论**：Par 版本在小图上**没有优势**，反而比 Ref 慢 10%。

### web-Google（大图，876K节点）

| 版本 | 时间 | vs Ref |
|------|------|--------|
| Ref | 416 ms | 1.00× |
| 优化单线程 | 328 ms | **1.27×** |
| 优化多线程 | 311 ms | **1.34×** |

**结论**：优化的普通版本在大图上有 **1.34× 的加速**。

## 问题分析

### 为什么 Par 版本在小图上更慢？

1. **Ref 版本已经很优化**：包含并行代码和优化的数据结构
2. **Par 版本的动态分配开销**：`byNewClique` 的哈希查找和动态扩展有开销
3. **小图上优势不明显**：动态分配节省的内存在小图上不重要

### 为什么优化版本在大图上更快？

1. **更好的并行策略**：我们优化了 `countingPerRCliqueParallel` 和 Part A
2. **减少了 critical sections**：使用线程局部缓冲区
3. **更好的内存管理**：预分配 + 批量合并

## 实际优化效果

### SDCT 优化

| 数据集 | 串行 | 并行 | 加速比 |
|--------|------|------|--------|
| com-dblp | 438 ms | 264 ms | **1.66×** |
| web-Google | 2329 ms | 1207 ms | **1.93×** |

SDCT 的优化是**成功的**，在两个数据集上都有显著加速。

### Nucleus Decomposition 优化

| 数据集 | Ref | 优化多线程 | 加速比 |
|--------|-----|------------|--------|
| com-dblp | 335 ms | 370 ms | **0.91×**（更慢） |
| web-Google | 416 ms | 311 ms | **1.34×** |

Nucleus 的优化在**大图上有效**，但在小图上反而更慢。

## 总体性能（包含 SDCT + Nucleus）

### com-dblp
- 优化前（估计）：438 + 335 = 773 ms
- 优化后：264 + 370 = 634 ms
- **总加速比：1.22×**

### web-Google
- 优化前（估计）：2329 + 416 = 2745 ms
- 优化后：1207 + 311 = 1518 ms
- **总加速比：1.81×**

## 诚实的结论

1. **SDCT 优化成功**：1.66-1.93× 加速
2. **Nucleus 优化有限**：
   - 小图：反而更慢（0.91×）
   - 大图：有一定加速（1.34×）
3. **总体效果**：
   - 小图：1.22× 加速（未达到 5× 目标）
   - 大图：1.81× 加速（未达到 5× 目标）

## 为什么没有达到 5× 目标？

1. **Ref 版本已经很优化**：不是纯串行的基准
2. **算法本质的串行依赖**：heap 操作、数据结构更新
3. **Amdahl 定律**：存在大量串行部分
4. **我们的"优化"引入了新开销**：
   - Par 版本的动态分配
   - 自适应选择的判断开销

## 真实的贡献

虽然没有达到 5× 目标，但我们：
1. ✅ 实现了 SDCT 的有效并行化（1.66-1.93×）
2. ✅ 在大图上优化了 Nucleus（1.34×）
3. ✅ 实现了自适应算法选择
4. ✅ 移除了一些 critical section 瓶颈
5. ✅ 所有优化都通过了正确性验证

这是一个**诚实但有限**的优化结果。
