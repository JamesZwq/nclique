\section{Experiments}\label{sec:experiments}
% Drafted per SigmodPlus.md 239. All numbers single-run scout protocol (E2 medians
% pending; refresh per ledger before submission). Case studies: TODO, continue Exp-N.
In this section, we evaluate the builder against the state of the art, and the index against the only baseline that answers the same queries.

\stitle{Hardware.}
All experiments run on a server with two Intel Xeon Gold 6342 processors and $503$\,GB of memory, on a single thread\footnote{Code will be released upon publication.}.
%
All algorithms are implemented in C++ and compiled with \textsf{-O3}.

\stitle{Algorithms.}
\cnd{} is the published state of the art \cite{NuclearCD}, run from its authors' implementation one cell at a time.
%
\buildalg{} is our plane builder (Algorithm~\ref{alg:build}); \queryalg{} is the index query (Algorithm~\ref{alg:query}).
%
The query baseline is the \emph{archive}: one sorted record per scored clique holding its values across the row, probed by binary search.
%
Every archive choice favors the archive: four-byte keys and values, rows counted at their exact number, and a workload that always hits.

\begin{table}[t]
\centering
\caption{Datasets: four domains, eight graphs.}\label{tab:datasets}
\small
\begin{tabular}{@{}llrr@{}}
\toprule
 & graph & $|V|$ & $|E|$\\
\midrule
web & \textsf{web-it-2004} (WI) & $509{,}338$ & $7{,}178{,}413$\\
web & \textsf{web-Google} (WG) & $875{,}713$ & $5{,}105{,}039$\\
collaboration & \textsf{com-dblp} (DB) & $317{,}080$ & $1{,}049{,}866$\\
co-purchase & \textsf{com-amazon} (AZ) & $334{,}863$ & $925{,}872$\\
mesh & \textsf{nasasrb} (NS) & $54{,}870$ & $1{,}311{,}227$\\
mesh & \textsf{pkustk11} (PK1) & $87{,}804$ & $2{,}565{,}054$\\
mesh & \textsf{pkustk13} (PK3) & $94{,}893$ & $3{,}260{,}967$\\
mesh & \textsf{pwtk} (PW) & $217{,}918$ & $5{,}708{,}253$\\
\bottomrule
\end{tabular}
\end{table}

\stitle{Datasets.}
Table~\ref{tab:datasets} lists the eight graphs: two web crawls \cite{nr,Stanford}, a collaboration network and a co-purchase network from SNAP \cite{Stanford}, and four finite-element meshes from the SuiteSparse collection \cite{suitesparse}.
The indexed plane is $r\in[3,5]$, $s\le8$ throughout; the grid experiments use the nine cells $s\le r+3$ of that plane.

\stitle{Metrics.}
Time is end-to-end and memory is the peak of the process, both of a single run on an idle machine.
%
\cnd{} receives a $1{,}800$-second budget and a $300$\,GB cap per cell.
%
A red $\times$ marks a cell over the budget, a red $-$ marks a memory kill, and the dotted line in the figures marks the budget itself.
%
Figures label a cell $(r,s)$ compactly as $rs$.
%
\buildalg{} computes a whole row per sweep, so a cell is charged its marginal time and the row's peak memory.
%
Times below the harness resolution of $0.01$ seconds are drawn at that resolution.

\expsection{Build time against the state of the art}
Figure~\ref{fig:gridtime} shows build time per grid cell on all eight graphs.
%
The y-axis is time in seconds on a log scale, and the x-axis lists the cells of each row.
%
\cnd{} completes $42$ of the $72$ cells; \buildalg{} completes all $72$.
%
It is faster on every one of the $42$.
%
The closest cell is WG at $(3,4)$ with $1.4\times$; the widest is DB at $(5,8)$ with $5{,}806\times$.
%
The gap widens along both parameters, exactly as the certificates predict.
%
Within a row, absorption turns later cells into arithmetic: on DB, $43.31$ seconds against $0.08$ at $(4,6)$.
%
Across rows, the diagonal starts each boundary warm.
%
On WI, \cnd{} finishes none of the nine cells, while our whole rows take $0.81$, $1.60$, and $3.18$ seconds.
%
On a single matched cell computed standalone, WI at $(3,4)$, \cnd{} needs $5{,}581$ seconds and $101$\,GB against our $7.5$ seconds and $0.45$\,GB.
%
That is $742\times$ in time and $226\times$ in memory.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig_grid_time.pdf}
\caption{Build time per grid cell (log scale).}
\label{fig:gridtime}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig_grid_memory.pdf}
\caption{Build memory per grid cell (log scale).}
\label{fig:gridmem}
\end{figure*}

\expsection{Build memory against the state of the art}
Figure~\ref{fig:gridmem} shows peak memory for the same cells.
%
\cnd's memory follows its scored-clique count and crosses $93$\,GB already on the one-million-edge DB at row $5$.
%
Two mesh cells and one WI cell die at the $300$\,GB cap.
%
Our peak follows the pattern count instead and stays between $36$\,MB and $57$\,GB across all $72$ cells.
%
The one graph where \cnd{} is leaner at $r=3$, WG, is the graph with the weakest quotient of the eight; its rows still win on time.

\expsection{Building the whole plane}
Table~\ref{tab:plane} reports the end-to-end cost of \buildalg{} for the full plane $r\in[3,5]$, $s\le8$.
%
Every graph builds in minutes, six of eight in under two, on one thread.
%
For scale, the one standalone \cnd{} cell of Exp-1 costs $207\times$ the whole WI plane.

\begin{table}[t]
\centering
\caption{Whole-plane build: time and peak memory.}\label{tab:plane}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
graph & time (s) & memory (GB)\\
\midrule
WI & $27.0$ & $1.7$\\
WG & $599.1$ & $25.8$\\
DB & $66.5$ & $5.2$\\
AZ & $3.6$ & $0.6$\\
NS & $87.8$ & $8.8$\\
PK1 & $12.7$ & $1.7$\\
PK3 & $217.1$ & $25.9$\\
PW & $70.6$ & $7.3$\\
\bottomrule
\end{tabular}
\end{table}

\expsection{Index size against the archive}
Table~\ref{tab:size} compares \nsi{} against the archive over the same plane.
%
The archive column counts its exact rows at four bytes per key and cell; the \nsi{} column is the file as written.
%
On WI the archive would occupy $50.8$\,TB across $1.59$ trillion rows, and for $r\ge4$ it cannot be materialized at all; \nsi{} answers the same queries from $1.70$\,MB.
%
The ratio tracks the quotient: it is seven orders of magnitude on WI and smallest on AZ, the graph whose archive is itself only $20.7$\,MB.

\begin{table}[t]
\centering
\caption{Size of the archive and of \nsi{} for the whole plane.}\label{tab:size}
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
graph & archive & \nsi & ratio\\
\midrule
WI & $50.8$\,TB & $1.70$\,MB & $3.0\times10^{7}$\\
PK3 & $86.4$\,GB & $0.51$\,MB & $1.7\times10^{5}$\\
PW & $58.5$\,GB & $1.21$\,MB & $4.8\times10^{4}$\\
PK1 & $28.2$\,GB & $0.32$\,MB & $8.8\times10^{4}$\\
NS & $9.9$\,GB & $0.51$\,MB & $1.9\times10^{4}$\\
DB & $8.5$\,GB & $1.65$\,MB & $5.1\times10^{3}$\\
WG & $4.9$\,GB & $8.81$\,MB & $5.5\times10^{2}$\\
AZ & $21.7$\,MB & $1.53$\,MB & $14$\\
\bottomrule
\end{tabular}
\end{table}

\expsection{Query latency against the archive}
Table~\ref{tab:query} reports the median point-query latency of \queryalg{} against the archive probe, on $20{,}000$ uniformly sampled scored cliques per row.
%
Warm queries repeat over a resident working set; cold queries follow a cache eviction.
%
\queryalg{} answers in $74$ to $433$ nanoseconds warm, and it wins $20$ of the $22$ comparisons the archive can enter, by up to $12.7\times$.
%
On WI at $r\ge4$ the archive cannot be built, so the index is the only structure that answers at all.
%
The archive wins only on AZ at $r\ge4$: its $22$\,MB fit in cache, and a graph whose archive fits in cache does not need an index.

\begin{table}[t]
\centering
\caption{Warm point-query latency (ns) and speedups over the archive probe.}\label{tab:query}
\tiny
\begin{tabular}{@{}lrrrrrrrrr@{}}
\toprule
 & \multicolumn{3}{c}{$r=3$} & \multicolumn{3}{c}{$r=4$} & \multicolumn{3}{c}{$r=5$}\\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}\cmidrule(lr){8-10}
graph & \nsi & arch. & speedup & \nsi & arch. & speedup & \nsi & arch. & speedup\\
\midrule
WI & $79$ & $942$ & $11.9\times$ & $74$ & --- & --- & $80$ & --- & ---\\
WG & $354$ & $632$ & $1.8\times$ & $401$ & $820$ & $2.0\times$ & $414$ & $1016$ & $2.5\times$\\
DB & $173$ & $397$ & $2.3\times$ & $132$ & $597$ & $4.5\times$ & $156$ & $1023$ & $6.6\times$\\
AZ & $376$ & $490$ & $1.3\times$ & $433$ & $388$ & $0.9\times$ & $408$ & $226$ & $0.6\times$\\
NS & $199$ & $632$ & $3.2\times$ & $354$ & $879$ & $2.5\times$ & $401$ & $1160$ & $2.9\times$\\
PK1 & $163$ & $708$ & $4.3\times$ & $292$ & $1031$ & $3.5\times$ & $344$ & $1372$ & $4.0\times$\\
PK3 & $282$ & $798$ & $2.8\times$ & $221$ & $1207$ & $5.5\times$ & $426$ & $1582$ & $3.7\times$\\
PW & $177$ & $829$ & $4.7\times$ & $185$ & $1143$ & $6.2\times$ & $204$ & $2598$ & $12.7\times$\\
\bottomrule
\end{tabular}
\end{table}

\expsection{The certificates, ablated}
Table~\ref{tab:ablation} disables each transfer theorem in turn and rebuilds the plane.
%
Without absorption, every cell above a boundary is peeled in full: NS slows by $22.6\times$, and PK3 exceeds a $5{,}400$-second budget against its $204$-second default.
%
Without the diagonal, every boundary is peeled cold: $2.4\times$ on NS and $3.0\times$ on PK3, with memory more than doubling.
%
The completed ablations return the same core-number distribution as the default build.

\begin{table}[t]
\centering
\caption{Whole-plane build time under ablations.}\label{tab:ablation}
\small
\begin{tabular}{@{}lrrr@{}}
\toprule
graph & default & no diagonal & no absorption\\
\midrule
NS & $83.7$\,s & $197.9$\,s\ \ ($2.4\times$) & $1{,}894.6$\,s\ \ ($22.6\times$)\\
PK3 & $204.5$\,s & $615.8$\,s\ \ ($3.0\times$) & $>5{,}400$\,s\ \ ($>26\times$)\\
\bottomrule
\end{tabular}
\end{table}

\expsection{Applicability, decided in advance}
The build test of Section~\ref{sec:index} was computed for every candidate graph before any build.
%
It accepted all eight graphs of Table~\ref{tab:datasets}, and it rejected the social networks: \pokec{} at a ratio of $1.001$ and \textsf{email-Eu-core} at $1.002$.
%
In those graphs the vertices are pairwise distinguishable, so the quotient is the graph itself.
%
The rejections cost the front end only, and no index was built to learn them.
\expsection{Case study: reading a collaboration network's ladder}
We read the whole plane of DB through the index.
%
Cells are compared at the same rank of their level ladder, not at the same $k$, since absolute levels are incomparable across cells.
%
\sstitle{The top of every cell is one paper.}
The four highest levels of every cell are the floors of four author lists: papers with $114$, $102$, $65$, and $63$ authors.
%
At $(3,4)$ the levels read $111$, $99$, $62$, $60$; at $(3,8)$ they read $128{,}164{,}707$, $71{,}523{,}144$, $6{,}471{,}002$, $5{,}461{,}512$.
%
The order never changes, because the floor is monotone in the local clique number at every cell.
%
\sstitle{The spacing is the resolution.}
What changes with $s$ is the spacing.
%
The top two levels sit $1.12\times$ apart at $s=4$ and $1.79\times$ apart at $s=8$, and the gap to the third tier grows from $1.6\times$ to $11\times$.
%
Retrieval shows what close levels cost at $(3,4)$.
%
Level $111$ selects one component of $240{,}464$ triangles, level $99$ adds the $102$-author paper as a second component, and level $62$ merges the $65$-author paper into the first.
%
Level $60$ then adds the $63$-author paper as a third, separate component.
%
\sstitle{Two integer levels catch a shared author list.}
The $65$-author level holds only $2{,}016$ triangles, which is $\binom{65}{3}-\binom{64}{3}$.
%
That paper shares $64$ of its $65$ authors with the $114$-author paper, and the plane separates the two lists by one ladder step.
%
\sstitle{Practical guideline.}
Exploration should start below the top level, where cells genuinely differ.
%
When two levels sit close at a small $s$, raising $s$ spreads them apart before a threshold has to be chosen.

\expsection{Case study: the residue names the teams}
We index the DBLP coauthor ego network of Jiawei Han: $1{,}358$ coauthors, $21{,}699$ edges, and $297{,}899$ scored triangles at $r=3$.
%
Call a triangle \emph{explained by one paper} if its value equals its floor at every cell; the exceptions are exactly the residue that \nsi{} stores.
%
\sstitle{The upper levels are consortium papers.}
As on DB, the top of every cell is a large author list.
%
The highest level of $(3,4)$ is $93$, the floor of a $96$-author paper whose $\binom{96}{3}=142{,}880$ triangles all share it.
%
\sstitle{The residue names the standing teams.}
Only $45$ of the $297{,}899$ triangles exceed their floor at $s=4$, spanning $53$ researchers, and they read as the standing collaborations around the ego.
%
The strongest is Charu Aggarwal, Jian Pei, and Xiaorui Liu.
%
Their triangle holds $6$ supporting $4$-cliques against a floor of $5$, and $14$ supporting $5$-cliques against a floor of $10$.
%
The others include a sensor-network circle (Kaplan, Szymanski, Abdelzaher), a language-processing circle (Ji, Gao, Zhu), and a cohort of the ego's recent students (Meng, Shen, Zhang, Mao).
%
\sstitle{Raising the witness size dissolves coincidence.}
The residue thins from $45$ patterns at $s=5$ to $38$ at $s=7$ and $9$ at $s=8$.
%
At $s=8$ a single triangle still exceeds its floor: Aggarwal, Pei, and Liu.
%
\sstitle{What the index keeps is the story.}
Across the three rows, $9.0$ million patterns produce $45$ stored records, all in row $3$, and those records are precisely the multi-paper teams.
%
Everything else is one paper's arithmetic, reconstructed on demand.
% NOTE: all case-study numbers are structural (counts, levels, names) and
% machine-independent; no timings are reported here.
