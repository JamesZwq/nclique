\section{Experiments}\label{sec:experiments}
% TODO: draft per SigmodPlus.md 239 (contract below) under the paper-architect skill.
% CONTRACT (239): four \stitle setup blocks then \expsection blocks.
% Exp-1 build vs serial CND grid (no losing cell; 742x/226x web-it (3,4)).
% Exp-2 whole-plane build on the roster (+ phase split).
% Exp-3 index size vs archive (14x-29.9Mx; unbuildable cells) and vs full plane.
% Exp-4 query latency kernel vs archive probe (74ns; com-amazon cell conceded and
%       explained by design: its archive fits in cache and needs no index).
% Exp-5 ablations: certificates (22.6x / 2.4-3.0x), NOCERT variants.
% Exp-6 applicability: W prediction table incl. predicted-negative social.
% Case studies continue Exp-N numbering: multi-resolution exploration demo.
% Lead figure: the three-panel feasibility/size/latency figure (from 237).
