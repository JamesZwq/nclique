\section{The Index and Its Query}\label{sec:index}
% TODO: draft per SigmodPlus.md 239 (contract below) under the paper-architect skill.
% CONTRACT (239): what is stored (quotient + exceptions); reconstruction lemma
% (cP = max |M| over profile intersection); corollary: certified patterns cost 0
% bytes; corollary: the kernel validates cliques for free; P10 residue lower bound
% (Theorem if formalizable, else Remark + "minimal in kind"); Alg 4 = Query (4
% lines); W definition + prediction table; byte anatomy 70.5-99.9%.
