\appendix
\section{Deferred Constructions}\label{app:deferred}

\subsection{Building the Boxes}\label{app:boxes}
% TODO: the class-level pivoted recursion (degeneracy-ordered, hold/pivot bounds),
% correctness of the partition, and the [smin, smax] size window. Cite \cite{Pivoter}.
The construction lifts the pivoting recursion of \cite{Pivoter} from vertices to classes and records hold and pivot bounds per class; details to be included in the full version of this appendix.

\subsection{Incremental Evaluation of the Filtered Coefficient}\label{app:delta}
% TODO: the credit rule -- enumerate compositions newly dominated by an inserted
% removal once, credit each co-hosted pattern by the product of remaining choices;
% equivalence with Equation (2); cost accounting.
Each insertion into $A_B$ enumerates only the compositions it newly dominates and credits the co-hosted patterns by closed-form products; details to be included in the full version of this appendix.
