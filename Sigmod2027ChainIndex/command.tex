% Macros for the chain-index paper.  House macros first (\stitle/\kw ...), paper-specific names below.
\newcommand{\stitle}[1]{{\noindent \bf {#1}}}
\newcommand{\sstitle}[1]{{\noindent \textit {\uline {#1}}}}   % 2026-09-22: breakable underline (ulem); same look as \underline
\newcommand{\kw}[1]{{\ensuremath {\mathsf{#1}}}\xspace}
\newcommand{\bkw}[1]{{\ensuremath {\mathsf{\textbf{#1}}}}\xspace}

\newcounter{expcnt}
\newcommand{\expsection}[1]{\vspace{0.6ex}\stepcounter{expcnt}\noindent\textbf{Exp-\arabic{expcnt}: #1.}\xspace}

% ---- paper-specific names ----
\newcommand{\chainidx}{\kw{ChainIndex}}          % the index (the paper's one branded artifact)
\newcommand{\strees}{\kw{STrees}}
\newcommand{\cnd}{\kw{CND}}
\newcommand{\query}{\hyperref[alg:query]{\kw{Query}}\xspace}
\newcommand{\build}{\hyperref[alg:build]{\kw{Build}}\xspace}                     % the published single-size baseline (counting-based nucleus decomposition)                 % the baseline: one tree and one DFS array per size
\newcommand{\core}[2]{\kappa_{#1}(#2)}            % core value kappa_s(v)
\newcommand{\own}[2]{X_{#1}(#2)}                  % own node
\newcommand{\omg}[1]{\omega(#1)}                  % largest clique containing #1
\newcommand{\sig}[1]{\sigma(#1)}                  % certification point
\newcommand{\chain}[1]{\mathrm{chain}(#1)}
\newcommand{\traj}[1]{\mathrm{traj}(#1)}           % trajectory: the tuple of own nodes over the sizes
\newcommand{\Tree}[1]{T_{#1}}
\newcommand{\Nuc}[2]{(#1,#2)\text{-core}}   % the clique core at clique size #1 and level #2 (a k-(1,s)-nucleus)
\newcommand{\binomc}[2]{\binom{#1}{#2}}
\newcommand{\dblp}{\textsf{com-dblp}\xspace}
\newcommand{\webuk}{\textsf{web-uk-2005}\xspace}
\newcommand{\emailcore}{\textsf{email-Eu-core}\xspace}
\input{tables/size_stats}
\input{tables/query_stats}
\input{tables/prior_stats}
\input{tables/layout_stats}
\input{tables/build_stats}   % 2026-09-23: Exp-1 and Section 7 cost numbers from the build-time ablation
\input{tables/selftest}
