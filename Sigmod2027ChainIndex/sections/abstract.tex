% 2026-09-21 house shape: the introduction compressed; challenge nouns and headline numbers verbatim.  Cadence pass.
% 2026-09-22 cold read 2: subject and verb together; the hierarchy sentence; the baseline not presented as prior practice; own node glossed; the peel glossed; the climb named; "decomposition" avoided.
The $k$-core of a graph counts the neighbours of a vertex.
%
Counting instead the cliques of $s$ vertices that contain it generalizes the $k$-core to every clique size $s$.
%
At each size every vertex gets a core value and lies in one community per level from $1$ to its core value, and the communities of a size are nested.
%
% 2026-09-22 compaction: thirteen sentences.
A community query names a vertex, a size and a level, and the size is chosen at query time.
%
The direct index for every size stores one hierarchy per size.
%
A vertex is then stored once per size, and the index reaches $190$~MB on a web graph of $129{,}632$ vertices.
%
This paper stores each vertex once for all sizes.
%
Two vertices are hierarchy-equivalent when no query, for a value or for a community, at any size tells them apart.
%
We prove that this holds exactly when, at every size, the smallest community containing one vertex, the one at its core value, is the smallest community containing the other.
%
The equivalence classes, called chains, partition the vertices, and no partition into larger groups allows an index to answer every query exactly.
%
We propose \chainidx, an index over chains.
%
It numbers the vertices so that every community, at any size and level, is a few ranges of consecutive vertex ids, found in constant time after a climb to the queried level.
%
The index of every size is built from one clique tree.
%
% 2026-09-23: the construction sentence describes the settled vertices (Theorem thm:settled), not the replay.
The values of a size come from removing vertices in increasing order of their clique counts, and a vertex whose value reached its floor at the previous size is not counted.
%
On \numgraphs{} graphs from five families, \chainidx is \ratiomin$\times$ to \ratiomax$\times$ smaller than one hierarchy per size (median \ratiomedian$\times$).
%
It finds the ranges of a community in \locmin{} to \locmax{} nanoseconds and lists its vertices about as fast as a memory copy of the answer.
%
It is built for every size \priormin$\times$ to \priormax$\times$ faster than computing the sizes one at a time.
%
Two case studies on graphs whose vertices carry names and categories show the effect of the size.
%
On DBLP the size whose community best matches a ground-truth community changes from query to query.
%
On Amazon the size narrows a community from a store of $157{,}331$ products to a shelf of six albums.
