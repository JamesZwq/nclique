# Pivoter 并行优化 - 最终诚实报告

## 优化成果（服务器，8线程）

### com-dblp（小图，317K节点）

| 组件 | 优化前 | 优化后 | 加速比 |
|------|--------|--------|--------|
| SDCT | 431 ms | 260 ms | **1.66×** |
| Nucleus | 404 ms | 418 ms | **0.97×**（更慢） |
| **总体** | **835 ms** | **678 ms** | **1.23×** |

### web-Google（大图，876K节点）

| 组件 | 优化前 | 优化后 | 加速比 |
|------|--------|--------|--------|
| SDCT | 2901 ms | 1263 ms | **2.30×** |
| Nucleus | 424 ms | 352 ms | **1.20×** |
| **总体** | **3325 ms** | **1615 ms** | **2.06×** |

## 实现的优化

### 1. SDCT 块并行化（1.66-2.30× 加速）✅
- 将顶点分成 N 块，每个线程处理连续块
- 保持串行逻辑中的状态累积
- 使用 `unique_ptr` 管理大数组

### 2. Nucleus countingPerRClique 优化（1.68-2.72× 加速）✅
- 移除 `#pragma omp critical`
- 使用线程局部数组 + 并行 reduction
- com-dblp: 273 ms → 163 ms
- web-Google: 67 ms → 25 ms

### 3. Nucleus Part A 优化（效果有限）
- 移除 `#pragma omp critical`
- 使用线程局部缓冲区 + 串行合并
- 小图上引入了开销（并行化不值得）
- 大图上有一定改善

## 为什么没有达到 5× 目标？

### 1. 算法的本质限制
- **主循环必须串行**：heap 操作、数据结构更新有严格的依赖关系
- **Amdahl 定律**：即使并行部分完美加速，串行部分限制了总体加速
- **小批量处理**：每次迭代处理的 cliques 很少，并行化机会有限

### 2. 并行化的开销
- **线程启动和同步**：在小图上，开销超过收益
- **缓存一致性**：多线程访问共享数据结构
- **负载不均衡**：不同迭代的工作量差异大

### 3. 数据依赖性
- 移除一个 clique 会影响其他 cliques 的 counting
- 必须按顺序处理（从小到大的 counting 值）
- 无法批量并行处理多个层级

## 真实的贡献

1. ✅ **SDCT 成功并行化**：1.66-2.30× 加速
2. ✅ **countingPerRClique 显著优化**：1.68-2.72× 加速
3. ✅ **大图总体性能提升**：2.06× 加速
4. ❌ **小图性能提升有限**：1.23× 加速
5. ❌ **未达到 5× 目标**

## 结论

### 成功的部分
- SDCT 的并行化非常成功，尤其在大图上（2.30×）
- countingPerRClique 的优化移除了 critical section 瓶颈
- 大图（web-Google）达到了 2.06× 的总体加速

### 失败的部分
- Nucleus 主循环的并行化效果极其有限（0.97-1.20×）
- 小图上并行化的开销超过收益
- 总体加速远低于 5× 目标

### 根本原因
**Nucleus Decomposition 算法的本质是串行的**。主循环中的 heap 操作和数据依赖关系使得大规模并行化几乎不可能。要达到 5× 加速，需要：

1. **完全不同的算法**：不基于 heap 的 peeling 算法
2. **GPU 加速**：利用大规模并行处理
3. **近似算法**：牺牲精确性换取速度
4. **或者接受这是算法的理论限制**

## 最终数据

| 数据集 | 优化前 | 优化后 | 加速比 | 达成目标？ |
|--------|--------|--------|--------|-----------|
| com-dblp | 835 ms | 678 ms | 1.23× | ❌ |
| web-Google | 3325 ms | 1615 ms | 2.06× | ❌ |

**结论**：虽然有一定改进，但远未达到 5× 的目标。这是一个诚实但令人失望的结果。
